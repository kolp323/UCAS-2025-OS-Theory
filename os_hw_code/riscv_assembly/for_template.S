    li t0, 1                # 初始化：将计数器 t0 设置为 1
    li t1, 10               # 初始化：将上限值 10 存入 t1
    
loop_start:
    bge t0, t1, end_loop    # 判断：如果 t0 >= t1，跳出循环
    
    # --- 循环体（这里可以放任何你想要重复执行的代码）---
    # 比如，你可以把 t0 的值打印出来
    # ...
    
    addi t0, t0, 1          # 递增：将计数器 t0 增加 1
    j loop_start            # 跳转：无条件跳转回循环的开头
    
end_loop:
    # ... 循环结束，程序从这里继续